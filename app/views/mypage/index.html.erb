<h1>トップページ</h1>
<% if @user.image.attached? %>
     <%= image_tag  @user.image, size: '250x200'%>
<% end %>
<% if @deadline.present? %>
  <p>目標日までの残り日数: <%= @remaining_days %>日</p>
<% else %>
  <p>目標日は設定されていません。</p>
<% end %>
<p>
  <%= link_to "勉強記録へ", study_records_path %>
  <%= link_to "模試記録へ", tweets_path %>
  <%= link_to "やる気アップ", youtubes_path %>
</p>

<% if user_signed_in? %>
  <p><%= link_to "マイページへ", user_path(current_user.id) %></>
<% end %>
<p><%= button_to 'ログアウト', destroy_user_session_path, method: :delete %> </p>